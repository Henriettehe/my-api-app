// Kilde: Kodeeksempel fra Fullstækk - videoforlesninger og forklaring.

// Fulgt videoer fra fullstækk, men søk på nettet sier og viser mye rart! Jeg er en smule forvirret men håper at ved gruppearbeid at ting løsner! :D 

import '../globals.css'; 

function MyApp({ Component, pageProps }) {
    return (
    <Layout>
        <Component {...pageProps} />
    </Layout>
    )
}

export default MyApp; 